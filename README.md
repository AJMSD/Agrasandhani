# Agrasandhani
Agrasandhani is an MQTT IoT gateway that compacts sensor updates (batch + dedup + compaction + adaptive rate) and stabilizes dashboards (last-known-good + freshness TTL), benchmarked against a naive forwarder under network impairments and QoS 0/1.
